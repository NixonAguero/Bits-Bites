import { Routes } from '@angular/router';

export const routes: Routes = [
/*
  { path: '', component: Home }, 

  { path: 'menu', component: Menu }, 
  
  // Comodín: Si escriben cualquier otra cosa, redirige al inicio
  { path: '**', redirectTo: '' } 
*/
];
